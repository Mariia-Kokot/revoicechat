services:
  nginx:
    image: nginx:alpine
    ports:
      - "${NGINX_PORT:-3000}:3000"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - revoicechat_network
    depends_on:
      - backend
networks:
  revoicechat_network:
    external: true